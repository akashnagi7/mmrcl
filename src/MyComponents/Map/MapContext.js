/* This code is creating a new context object called `MapContext` using the `createContext()` method
from the React library. */
import React from "react";
const MapContext = new React.createContext();
export default MapContext;
