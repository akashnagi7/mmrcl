/**
 * The above code is a React component called "Layers" that renders its children inside a div.
 */
import React from "react";
const Layers = ({ children }) => {
  return <div>{children}</div>;
};
export default Layers;
